I need to completely redesign the PropertyCard component to look like a modern investor listing.

Please replace the existing property card code with the new React component code I am providing below.

Design Requirements that are handled in the code:

Image Layer: The address and city must be overlayed on the bottom of the image using a gradient scrim so white text is readable. The Status Badge goes in the top left.

Financial Row: "Purchase Price" and "Est. Equity" need to be on the same row immediately below the image.

Icons: Use the inline SVGs provided in the code (no external icon libraries).

No Funding Bar: Do not include the progress bar.

Button: A full-width "View Details" button at the bottom.

Here is the code to use: [PASTE THE CODE BLOCK FROM STEP 2 HERE]

Step 2: The Complete Code
Copy this code block and paste it at the end of the prompt above.

JavaScript

import React from 'react';

// You can pass the property data as a prop named 'property'
const PropertyCard = ({ property }) => {
  // Fallback data in case props aren't connected yet
  const data = property || {
    address: "133 Worchester Cir",
    cityStateZip: "Perry, GA 31069",
    imageUrl: "https://placehold.co/600x400", // Replace with real image source
    status: "Funding Committed",
    price: 165000,
    equity: 90000,
    beds: 3,
    baths: 2,
    sqft: 1608,
    closeDate: "Dec 21, 2025",
    bpo: 255000
  };

  // Helper to format currency
  const formatMoney = (amount) => {
    return new Intl.NumberFormat('en-US', {
      style: 'currency',
      currency: 'USD',
      maximumFractionDigits: 0,
    }).format(amount);
  };

  return (
    <div className="bg-white rounded-xl shadow-md overflow-hidden hover:shadow-lg transition-shadow duration-300 flex flex-col w-full max-w-sm mx-auto font-sans border border-gray-100">
      
      {/* 1. IMAGE SECTION with OVERLAYS */}
      <div className="relative h-56 w-full group cursor-pointer">
        <img 
          src={data.imageUrl} 
          alt={data.address} 
          className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" 
        />
        
        {/* Status Badge */}
        <div className="absolute top-3 left-3 bg-yellow-500 text-white text-[10px] uppercase font-bold px-2 py-1 rounded shadow-sm tracking-wide">
          {data.status}
        </div>

        {/* Gradient Scrim for Address Readability */}
        <div className="absolute bottom-0 w-full bg-gradient-to-t from-black/90 via-black/50 to-transparent p-4 pt-16">
          <h3 className="text-white text-xl font-bold leading-tight drop-shadow-md">
            {data.address}
          </h3>
          <p className="text-gray-200 text-sm drop-shadow-sm opacity-90">
            {data.cityStateZip}
          </p>
        </div>
      </div>

      {/* 2. CARD BODY */}
      <div className="p-4 flex flex-col gap-5">

        {/* Financials Row */}
        <div className="flex justify-between items-end border-b border-gray-100 pb-3">
          <div>
            <p className="text-[10px] text-gray-500 uppercase font-bold tracking-wider mb-0.5">
              Purchase Price
            </p>
            <p className="text-2xl font-bold text-gray-900">
              {formatMoney(data.price)}
            </p>
          </div>
          <div className="text-right">
            <p className="text-[10px] text-green-600 uppercase font-bold tracking-wider mb-0.5">
              Est. Equity
            </p>
            <p className="text-2xl font-bold text-green-600">
              +{formatMoney(data.equity)}
            </p>
          </div>
        </div>

        {/* Stats Row with Inline SVG Icons */}
        <div className="flex items-center justify-start text-gray-500 text-sm font-medium">
          
          {/* Bed */}
          <div className="flex items-center gap-2 pr-4 border-r border-gray-200">
            <svg className="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
            </svg>
            <span>{data.beds} bd</span>
          </div>

          {/* Bath */}
          <div className="flex items-center gap-2 px-4 border-r border-gray-200">
            <svg className="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
            </svg>
            <span>{data.baths} ba</span>
          </div>

          {/* Sqft */}
          <div className="flex items-center gap-2 pl-4">
            <svg className="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M4 8V4m0 0h4M4 4l5 5m11-1V4m0 0h-4m4 0l-5 5M4 16v4m0 0h4m-4 0l5-5m11 5l-5-5m5 5v-4m0 4h-4"></path>
            </svg>
            <span>{data.sqft.toLocaleString()} sf</span>
          </div>
        </div>

        {/* Info/Dates Row */}
        <div className="flex justify-between items-center text-xs mt-1">
          <div className="flex items-center gap-1.5 text-gray-400 font-medium">
            {/* Clock Icon */}
            <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <span>Closes {data.closeDate}</span>
          </div>
          <div className="flex items-center gap-1.5 text-blue-600 font-bold bg-blue-50 px-2 py-1 rounded">
            {/* Chart Icon */}
            <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
            </svg>
            <span>BPO: {formatMoney(data.bpo)}</span>
          </div>
        </div>

        {/* 3. ACTION BUTTON */}
        <button className="group w-full bg-blue-600 hover:bg-blue-700 active:bg-blue-800 text-white font-bold py-3.5 rounded-lg flex justify-center items-center gap-2 transition-all shadow-blue-200 shadow-md hover:shadow-lg mt-1 cursor-pointer">
          View Details 
          {/* Chevron Right */}
          <svg className="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </button>

      </div>
    </div>
  );
};

export default PropertyCard;
3 Technical Details This Code Fixes
React className: I ensured all classes use className instead of class (which breaks in React).

Dynamic Data Mapping: I added a const data = ... section at the top. Replit will see this and understand how to map your database fields (like price, address, etc.) to the visual parts of the card.

Currency Formatting: I included a helper function formatMoney so your prices automatically look like $165,000 instead of 165000.